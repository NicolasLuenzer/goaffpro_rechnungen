---
phase: 02-versch-nerung
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/main/resources/ui/dashboard.html
autonomous: true
requirements:
  - BEAU-10

must_haves:
  truths:
    - "Typography uses at most 4-5 distinct font-size values in CSS rules (12px, 14px, 18px, 24px, plus 10px exception for side-nav-section-label)"
    - "All font-size values reference CSS custom property tokens (--fs-sm, --fs-body, --fs-sub, --fs-heading)"
    - "The .side-nav-section-label retains its small size (exception noted)"
  artifacts:
    - path: "src/main/resources/ui/dashboard.html"
      provides: "Typography scale tokens and consolidated font sizes"
      contains: "--fs-sm"
  key_links:
    - from: ":root"
      to: "CSS font-size rules"
      via: "Typography tokens --fs-sm, --fs-body, --fs-sub, --fs-heading"
      pattern: "--fs-sm|--fs-body|--fs-sub|--fs-heading"
    - from: "font-size declarations"
      to: "var(--fs-*)"
      via: "All font-size rules use tokens instead of hardcoded px"
      pattern: "font-size:\\s*var\\(--fs-"
---

<objective>
Consolidate 8 distinct font sizes (10px through 26px) into a 4-step typography scale using CSS custom property tokens. This is the final visual polish step that requires Plan 02-02's token system to be in place.

Purpose: Eliminate the 8 arbitrary font sizes creating visual noise (audit finding: Typography 2/4). Replace with a disciplined 4-step scale: 12px (body-small), 14px (body), 18px (subheading), 24px (heading). Exception: keep .side-nav-section-label at 10px (WCAG note accepted).
Output: Typography tokens in :root, all font-size rules using tokens, at most 4-5 distinct sizes in the file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-versch-nerung/02-RESEARCH.md
@.planning/phases/02-versch-nerung/02-02-SUMMARY.md

<interfaces>
<!-- After Plan 02-02, the :root block will have expanded tokens. Typography tokens will be added to this block. -->

Current font-size usage inventory (from grep):
```
12 occurrences: font-size:12px
7 occurrences:  font-size:13px
6 occurrences:  font-size: 13px
4 occurrences:  font-size: 12px
3 occurrences:  font-size: 14px
2 occurrences:  font-size:15px
2 occurrences:  font-size:11px
1 occurrence:   font-size:20px
1 occurrence:   font-size:10px
1 occurrence:   font-size: 26px
1 occurrence:   font-size: 22px (hamburger btn, responsive mobile only)
1 occurrence:   font-size: 20px (responsive mobile header h1)
1 occurrence:   font-size: 11px (responsive mobile header subtitle)
```

Font-size by component (CSS rules, NOT inline styles):
| Current | Component | New Token | New Value |
|---------|-----------|-----------|-----------|
| 10px | .side-nav-section-label | EXCEPTION - keep 10px | 10px |
| 11px | .side-nav-chevron | --fs-sm | 12px |
| 12px | .pill, .tree-toggle, .settings-hint, .mail-log-badge, .app-version, .advisor-hover-card, canvas | --fs-sm | 12px |
| 13px | .tab, .section-tab, .validation-subtab, .email-designer-tab, .side-nav-item, .side-nav-dept-badge, .side-nav-dept-name, .files, .status p, .help-doc, .table-filter-menu h4, .table-filter-options label, .version-popup li, .app-subtitle, th/td | --fs-body | 14px |
| 14px | input, button, .status div | --fs-body | 14px |
| 15px | .settings-group h3 | --fs-sub | 18px |
| 20px | settingsOverlay h2 (inline style line 400) | --fs-sub | 18px |
| 22px | .hamburger-btn | --fs-sub | keep 22px (it's the hamburger icon, not text) |
| 26px | h1 | --fs-heading | 24px |

Inline style font sizes to address:
- Line 400: `font-size:20px` on settings overlay h2 -> change to 18px
- Line 484 (research): `font-size:15px` on "Vorhandene Benutzer" strong -> change to `font-size:var(--fs-body)` (14px)
- Line 485: `font-size:13px` on reload button -> change to `font-size:var(--fs-sm)` (12px)

Note on responsive @media block:
- `.app-header h1 { font-size: 20px; }` inside @media -- this is a mobile override for h1. Change to `font-size: var(--fs-sub)` (18px) since that's the sub-heading scale.
- `.app-header .app-subtitle { font-size: 11px; }` inside @media -- change to `font-size: var(--fs-sm)` (12px).
- `.hamburger-btn font-size: 22px` -- keep as-is (this is the hamburger icon glyph size, not text typography).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typography tokens to :root and replace all font-size values in CSS rules</name>
  <files>src/main/resources/ui/dashboard.html</files>
  <read_first>
    - src/main/resources/ui/dashboard.html (lines 11-35 for :root block after 02-02, full CSS section for all font-size rules, lines 293-362 for @media block)
  </read_first>
  <action>
**Step 1: Add typography tokens to `:root` block.**

After the shadow tokens (after `--shadow-lg: ...;`) and before the closing `}`, add:

```css
      --fs-sm: 12px;
      --fs-body: 14px;
      --fs-sub: 18px;
      --fs-heading: 24px;
```

**Step 2: Replace font-size values in CSS rules.**

For each CSS rule in the `<style>` block, replace hardcoded font-size values. Process systematically from top to bottom:

**h1 (line 36):**
`font-size: 26px;` -> `font-size: var(--fs-heading);`

**.app-subtitle (line 42):**
`font-size: 13px;` -> `font-size: var(--fs-body);`

**.tab (around line 58 -- has `font-size:13px`):**
`font-size:13px;` -> `font-size:var(--fs-body);`

**.toolbar label, .toolbar strong (line 66 -- if font-size:13px exists):**
Check and replace any `font-size:13px` with `font-size:var(--fs-body)`.

**.settings-group h3 (line 73):**
`font-size:15px;` -> `font-size:var(--fs-sub);`

**.settings-hint (line 74):**
`font-size:12px;` -> `font-size:var(--fs-sm);`

**input global rule (line 77):**
`font-size: 14px;` -> `font-size: var(--fs-body);`

**button global rule (line 81):**
`font-size: 14px;` -> `font-size: var(--fs-body);`

**th, td (line 91):**
`font-size: 13px;` -> `font-size: var(--fs-body);`

**.files (line 98):**
`font-size: 13px;` -> `font-size: var(--fs-body);`

**.pill (line 102):**
`font-size:12px;` -> `font-size:var(--fs-sm);`

**.pill button (line 103):**
`font-size:12px;` -> `font-size:var(--fs-sm);`

**.request-preview (line 118):**
`font-size: 12px;` -> `font-size: var(--fs-sm);`

**.section-tab (around line 121, wherever font-size:13px):**
`font-size:13px;` -> `font-size:var(--fs-body);`

**.help-doc (line 129):**
`font-size:13px;` -> `font-size:var(--fs-body);`

**.app-version (line -- has font-size: 12px):**
`font-size: 12px;` -> `font-size: var(--fs-sm);`

**.save-btn-* rules:** No font-size, skip.

**.validation-subtab (around line 144, wherever font-size:13px):**
`font-size:13px;` -> `font-size:var(--fs-body);`

**.tree-toggle (line 153):**
`font-size: 12px;` -> `font-size: var(--fs-sm);`

**.side-nav-section-label (wherever font-size:10px):**
KEEP `font-size:10px` -- this is the intentional exception. Do NOT replace.

**.side-nav-chevron (wherever font-size:11px):**
`font-size:11px;` -> `font-size:var(--fs-sm);`

**.side-nav-item (wherever font-size:13px):**
`font-size:13px;` -> `font-size:var(--fs-body);`

**.side-nav-dept-badge (wherever font-size:12px):**
`font-size:12px;` -> `font-size:var(--fs-sm);`

**.side-nav-dept-name (wherever font-size:13px):**
`font-size:13px;` -> `font-size:var(--fs-body);`

**.email-designer-tab (around line 178, wherever font-size:13px):**
`font-size:13px;` -> `font-size:var(--fs-body);`

**.status (wherever font-size:13px):**
`font-size:13px;` -> `font-size:var(--fs-body);`

**.mail-log-badge (line 229):**
`font-size:12px;` -> `font-size:var(--fs-sm);`

**.table-filter-menu h4 (wherever font-size:13px):**
`font-size:13px;` -> `font-size:var(--fs-body);`

**.table-filter-options label (wherever font-size:13px):**
`font-size:13px;` -> `font-size:var(--fs-body);`

**.version-popup li (wherever font-size:13px):**
`font-size:13px;` -> `font-size:var(--fs-body);`

**.advisor-hover-card (wherever font-size:12px):**
`font-size:12px;` -> `font-size:var(--fs-sm);`

**.kpi-value, .kpi-label (check for font sizes):**
Replace any 12px/13px with `var(--fs-sm)` / `var(--fs-body)` as appropriate.

**Step 3: Update font sizes in @media (max-width: 767px) block:**

`.app-header h1 { font-size: 20px; }` -> `.app-header h1 { font-size: var(--fs-sub); }`
`.app-header .app-subtitle { font-size: 11px; }` -> `.app-header .app-subtitle { font-size: var(--fs-sm); }`

Do NOT change `.hamburger-btn { font-size: 22px; }` -- the hamburger icon glyph needs this size.

**Step 4: Update inline style font sizes in HTML:**

- Line 400: `font-size:20px` on the settings overlay h2 -> change to `font-size:18px` (cannot use var() because this is an inline style within a complex style attribute; use the literal value 18px matching --fs-sub)
- Line 484: `font-size:15px` on "Vorhandene Benutzer" strong -> change to `font-size:14px` (matching --fs-body)
- Line 485: `font-size:13px` on reload button -> change to `font-size:12px` (matching --fs-sm)

**Important:**
- The `.settings-group h3` goes from 15px to 18px (var(--fs-sub)). This is a deliberate step-up to create clear hierarchy between body text (14px) and section headings (18px).
- The h1 goes from 26px to 24px (var(--fs-heading)). Barely noticeable.
- All 13px text goes to 14px (var(--fs-body)). This is the main normalization -- 13px and 14px were indistinguishable.
- All 11px text goes to 12px (var(--fs-sm)). Also indistinguishable.
- `.side-nav-section-label` at 10px is the ONE exception -- keep it as the tiny uppercase label it is.
  </action>
  <verify>
    <automated>cd /c/Users/nluenzer/git/goaffpro_rechnungen && echo "=== Typography tokens ===" && grep -c "\-\-fs-sm\|\-\-fs-body\|\-\-fs-sub\|\-\-fs-heading" src/main/resources/ui/dashboard.html && echo "=== Distinct font sizes ===" && grep -oE "font-size:\s*[0-9]+px" src/main/resources/ui/dashboard.html | sort -u && echo "=== Token usage ===" && grep -c "var(--fs-" src/main/resources/ui/dashboard.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\-\-fs-sm: 12px" src/main/resources/ui/dashboard.html` returns a match in :root
    - `grep "\-\-fs-body: 14px" src/main/resources/ui/dashboard.html` returns a match in :root
    - `grep "\-\-fs-sub: 18px" src/main/resources/ui/dashboard.html` returns a match in :root
    - `grep "\-\-fs-heading: 24px" src/main/resources/ui/dashboard.html` returns a match in :root
    - `grep -oE "font-size:\s*[0-9]+px" src/main/resources/ui/dashboard.html | sort -u` produces at most 5 distinct values: 10px (side-nav-section-label exception), 12px (inline styles matching --fs-sm), 14px (inline styles matching --fs-body), 18px (inline styles matching --fs-sub), and 22px (hamburger icon)
    - `grep -c "var(--fs-" src/main/resources/ui/dashboard.html` returns at least 25 (the many CSS rules now using typography tokens)
    - `grep "font-size:10px" src/main/resources/ui/dashboard.html` returns exactly 1 match (side-nav-section-label)
    - `grep "font-size: 26px\|font-size:26px" src/main/resources/ui/dashboard.html` returns 0 matches (replaced with --fs-heading)
    - `grep "font-size: 15px\|font-size:15px" src/main/resources/ui/dashboard.html` returns 0 matches in CSS rules (replaced with --fs-sub; inline styles use 14px)
    - `grep "font-size: 13px\|font-size:13px" src/main/resources/ui/dashboard.html` returns 0 matches (all replaced with --fs-body)
    - `grep "font-size: 11px\|font-size:11px" src/main/resources/ui/dashboard.html` returns 0 matches (all replaced with --fs-sm)
  </acceptance_criteria>
  <done>
    Typography consolidated from 8 sizes to 4-step scale (12/14/18/24px) via CSS tokens. All CSS rules use var(--fs-*) tokens. Side-nav-section-label at 10px retained as documented exception. Hamburger icon at 22px retained (not typography). Inline styles use literal matching values.
  </done>
</task>

</tasks>

<verification>
- `grep -oE 'font-size:\s*[0-9]+px' src/main/resources/ui/dashboard.html | sort -u | wc -l` returns at most 5
- `:root` block contains --fs-sm, --fs-body, --fs-sub, --fs-heading
- `grep -c 'var(--fs-' src/main/resources/ui/dashboard.html` returns 25+
- No `font-size: 26px`, no `font-size: 15px`, no `font-size: 13px`, no `font-size: 11px` remaining
- Only exception: `font-size:10px` on `.side-nav-section-label`
</verification>

<success_criteria>
- 4 typography tokens defined in :root
- All font-size rules use tokens (except 10px exception and 22px hamburger icon)
- Visual hierarchy improved: sm(12) < body(14) < sub(18) < heading(24)
- No more than 5 distinct pixel sizes in the file
</success_criteria>

<output>
After completion, create `.planning/phases/02-versch-nerung/02-03-SUMMARY.md`
</output>
