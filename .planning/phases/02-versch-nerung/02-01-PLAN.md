---
phase: 02-versch-nerung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/ui/dashboard.html
autonomous: true
requirements:
  - BEAU-01
  - BEAU-02
  - BEAU-03
  - BEAU-04
  - BEAU-05
  - BEAU-06
  - BEAU-07

must_haves:
  truths:
    - "Browser tab shows a favicon (no 404 in console for /favicon.ico)"
    - "Login password field is inside a <form> element (browser can save credentials)"
    - "GoAffPro API-Key and ERPNext API-Key inputs are type=password by default"
    - "Beraterinnen-E-Mail label text is NOT red (uses neutral text color)"
    - "Date range inputs (von/bis) in Auswertungen stay on one line at 1440px"
    - "Chart empty state shows centered descriptive text instead of top-left 'Keine Daten'"
    - "Color picker inputs are at least 44x44px (WCAG 2.5.5)"
    - "Login button text is 'Anmelden' (German UX convention, not English 'Login')"
  artifacts:
    - path: "src/main/resources/ui/dashboard.html"
      provides: "Favicon link, form wrapper, password types, label fix, date layout, chart text, color picker size"
      contains: "rel=\"icon\""
  key_links:
    - from: "<head>"
      to: "browser tab"
      via: "<link rel=\"icon\" href=\"data:image/svg+xml,...\">"
      pattern: "rel=\"icon\""
    - from: "login-card"
      to: "<form>"
      via: "form wrapper around login inputs"
      pattern: "<form.*loginOverlay|login-card"
    - from: "#goaffproAPIKey"
      to: "type=\"password\""
      via: "input type attribute"
      pattern: "goaffproAPIKey.*type=\"password\""
    - from: ".recipient-advisor-label"
      to: "var(--clr-text)"
      via: "CSS color rule"
      pattern: "recipient-advisor-label.*clr-text"
---

<objective>
Apply 7 small, isolated UI/UX fixes to dashboard.html: favicon (data URI), login form wrapper, API-key password masking, Beraterinnen-E-Mail label color, date range layout, chart empty states, and color picker sizing.

Purpose: Close the 7 low-risk audit findings (BEAU-01 through BEAU-07) in a single pass. All changes are independent and touch different sections of the file.
Output: dashboard.html with all 7 fixes applied, verifiable by grep and browser inspection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-versch-nerung/02-RESEARCH.md

<interfaces>
<!-- Current <head> section (lines 0-9) -->
From dashboard.html line 0-9:
```html
<!doctype html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>S+R Assistent</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>
```

<!-- Login overlay (lines 505-513) -->
From dashboard.html lines 505-513:
```html
  <div id="loginOverlay" class="login-overlay">
    <div class="login-card">
      <h3 style="margin-top:0">Anmeldung</h3>
      <div class="toolbar"><label>Benutzername</label><input id="loginUsername" type="text" style="min-width:100%"/></div>
      <div class="toolbar"><label>Passwort</label><input id="loginPassword" type="password" style="min-width:100%"/></div>
      <div class="toolbar"><button id="loginBtn" type="button">Login</button><span id="loginStatus" class="status"></span></div>
      <div id="adminUsersBox" style="display:none;margin-top:12px;"></div>
    </div>
  </div>
```

<!-- GoAffPro API-Key input (line 692) -->
From dashboard.html line 692:
```html
<input id="goaffproAPIKey" type="text" placeholder="API-Key" />
```

<!-- ERPNext API-Key input (line 436) -->
From dashboard.html line 436:
```html
<input id="erpnextApiKey" type="text" placeholder="z. B. abc123..." style="min-width:320px;" />
```

<!-- Beraterinnen-E-Mail label CSS (line 107) -->
From dashboard.html line 107:
```css
.recipient-advisor-label { color: #b91c1c; font-weight: 700; }
```

<!-- Analytics date range (lines 837-840) -->
From dashboard.html lines 837-840:
```html
<label for="analyticsFromDate">von:</label>
<input id="analyticsFromDate" type="date" style="min-width:170px" />
<label for="analyticsToDate">bis:</label>
<input id="analyticsToDate" type="date" style="min-width:170px" />
```

<!-- drawBarChart empty state (lines 2292-2296) -->
From dashboard.html lines 2292-2296:
```javascript
if (!labels || labels.length === 0) {
  ctx.fillStyle = '#6b7280';
  ctx.font = '14px Arial';
  ctx.fillText('Keine Daten', 20, 30);
  return;
}
```

<!-- Color picker inputs (lines 776, 778) -->
From dashboard.html lines 776, 778:
```html
<label style="display:flex;align-items:center;gap:6px;">Textfarbe <input id="emailTextColor" type="color" value="#1f2937"/></label>
<label style="display:flex;align-items:center;gap:6px;">Highlight <input id="emailHighlightColor" type="color" value="#fff59d"/></label>
```

<!-- Login button (line 510) -->
From dashboard.html line 510:
```html
<button id="loginBtn" type="button">Login</button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Favicon, login form wrapper, API-key masking, and login button text</name>
  <files>src/main/resources/ui/dashboard.html</files>
  <read_first>
    - src/main/resources/ui/dashboard.html (lines 0-9 for head section, lines 505-513 for login overlay, lines 436 for erpnextApiKey, lines 692 for goaffproAPIKey)
  </read_first>
  <action>
Make the following 4 changes to dashboard.html:

**1. Add favicon data URI in `<head>` (after line 8, before the `<style>` line):**

Insert this line after `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />` and before `<style>`:

```html
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>📊</text></svg>">
```

**2. Wrap login card content in `<form>` (lines 505-513):**

Change the login overlay from:
```html
  <div id="loginOverlay" class="login-overlay">
    <div class="login-card">
      <h3 style="margin-top:0">Anmeldung</h3>
      <div class="toolbar"><label>Benutzername</label><input id="loginUsername" type="text" style="min-width:100%"/></div>
      <div class="toolbar"><label>Passwort</label><input id="loginPassword" type="password" style="min-width:100%"/></div>
      <div class="toolbar"><button id="loginBtn" type="button">Login</button><span id="loginStatus" class="status"></span></div>
      <div id="adminUsersBox" style="display:none;margin-top:12px;"></div>
    </div>
  </div>
```

To:
```html
  <div id="loginOverlay" class="login-overlay">
    <div class="login-card">
      <form onsubmit="return false;" autocomplete="on">
      <h3 style="margin-top:0">Anmeldung</h3>
      <div class="toolbar"><label for="loginUsername">Benutzername</label><input id="loginUsername" type="text" autocomplete="username" style="min-width:100%"/></div>
      <div class="toolbar"><label for="loginPassword">Passwort</label><input id="loginPassword" type="password" autocomplete="current-password" style="min-width:100%"/></div>
      <div class="toolbar"><button id="loginBtn" type="button">Anmelden</button><span id="loginStatus" class="status"></span></div>
      </form>
      <div id="adminUsersBox" style="display:none;margin-top:12px;"></div>
    </div>
  </div>
```

Key changes: (a) `<form onsubmit="return false;" autocomplete="on">` wraps the inputs and button, (b) `for` attributes on labels, (c) `autocomplete="username"` on username input, (d) `autocomplete="current-password"` on password input, (e) button text changed from "Login" to "Anmelden". Note: `adminUsersBox` stays OUTSIDE the form (it's a separate admin section).

**3. Change GoAffPro API-Key to type="password" (line 692):**

Change:
```html
<input id="goaffproAPIKey" type="text" placeholder="API-Key" />
```
To:
```html
<input id="goaffproAPIKey" type="password" placeholder="API-Key" />
```

**4. Change ERPNext API-Key to type="password" (line 436):**

Change:
```html
<input id="erpnextApiKey" type="text" placeholder="z. B. abc123..." style="min-width:320px;" />
```
To:
```html
<input id="erpnextApiKey" type="password" placeholder="z. B. abc123..." style="min-width:320px;" />
```

Do NOT touch `#smtpPassword` (line 421) or `#erpnextApiSecret` (line 440) -- they are already `type="password"`.
  </action>
  <verify>
    <automated>cd /c/Users/nluenzer/git/goaffpro_rechnungen && grep 'rel="icon"' src/main/resources/ui/dashboard.html && grep '<form' src/main/resources/ui/dashboard.html && grep 'goaffproAPIKey.*type="password"' src/main/resources/ui/dashboard.html && grep 'erpnextApiKey.*type="password"' src/main/resources/ui/dashboard.html && grep 'autocomplete="username"' src/main/resources/ui/dashboard.html && grep 'Anmelden' src/main/resources/ui/dashboard.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'rel="icon"' src/main/resources/ui/dashboard.html` returns 1
    - `grep 'rel="icon"' src/main/resources/ui/dashboard.html` contains `data:image/svg+xml`
    - `grep -c '<form' src/main/resources/ui/dashboard.html` returns 1
    - `grep '<form' src/main/resources/ui/dashboard.html` contains `onsubmit="return false;"` and `autocomplete="on"`
    - `grep 'goaffproAPIKey' src/main/resources/ui/dashboard.html | head -1` contains `type="password"`
    - `grep 'erpnextApiKey.*type=' src/main/resources/ui/dashboard.html | head -1` contains `type="password"`
    - `grep 'autocomplete="username"' src/main/resources/ui/dashboard.html` returns a match on the loginUsername input
    - `grep 'autocomplete="current-password"' src/main/resources/ui/dashboard.html` returns a match on the loginPassword input
    - `grep 'loginBtn' src/main/resources/ui/dashboard.html` contains text `Anmelden` (not `Login`)
    - `grep 'for="loginUsername"' src/main/resources/ui/dashboard.html` returns a match
    - `grep 'for="loginPassword"' src/main/resources/ui/dashboard.html` returns a match
    - `grep 'smtpPassword.*type="password"' src/main/resources/ui/dashboard.html` still exists (unchanged)
    - `grep 'erpnextApiSecret.*type="password"' src/main/resources/ui/dashboard.html` still exists (unchanged)
  </acceptance_criteria>
  <done>
    Favicon data URI in head (no 404). Login inputs in a form with autocomplete attributes and for-labels. GoAffPro and ERPNext API-Key inputs masked as type="password". Login button text is "Anmelden".
  </done>
</task>

<task type="auto">
  <name>Task 2: Beraterinnen-E-Mail label color, date layout, chart empty states, and color picker sizing</name>
  <files>src/main/resources/ui/dashboard.html</files>
  <read_first>
    - src/main/resources/ui/dashboard.html (line 107 for .recipient-advisor-label CSS, lines 825-842 for analytics toolbar HTML, lines 2280-2297 for drawBarChart JS, lines 776-779 for color picker HTML)
  </read_first>
  <action>
Make the following 4 changes to dashboard.html:

**1. Fix .recipient-advisor-label color (CSS line 107):**

Change:
```css
.recipient-advisor-label { color: #b91c1c; font-weight: 700; }
```
To:
```css
.recipient-advisor-label { color: var(--clr-text); font-weight: 700; }
```

This removes the red "error" appearance. The warning context is already communicated by the `.warning-banner` which shows when advisor-mode email is active.

**2. Wrap date range in a non-breaking flex group (lines 837-840):**

Find the four consecutive lines:
```html
          <label for="analyticsFromDate">von:</label>
          <input id="analyticsFromDate" type="date" style="min-width:170px" />
          <label for="analyticsToDate">bis:</label>
          <input id="analyticsToDate" type="date" style="min-width:170px" />
```

Replace with:
```html
          <div style="display:flex;align-items:center;gap:8px;flex-shrink:0;">
            <label for="analyticsFromDate">von:</label>
            <input id="analyticsFromDate" type="date" style="min-width:140px" />
            <label for="analyticsToDate">bis:</label>
            <input id="analyticsToDate" type="date" style="min-width:140px" />
          </div>
```

The wrapping div uses `flex-shrink:0` so the date range stays as a single non-breaking unit within the flex toolbar. The `min-width` is reduced from 170px to 140px to fit better.

**3. Improve drawBarChart empty state (JS lines 2292-2296):**

Find:
```javascript
      if (!labels || labels.length === 0) {
        ctx.fillStyle = '#6b7280';
        ctx.font = '14px Arial';
        ctx.fillText('Keine Daten', 20, 30);
        return;
      }
```

Replace with:
```javascript
      if (!labels || labels.length === 0) {
        ctx.fillStyle = '#9ca3af';
        ctx.font = '14px Inter, system-ui, sans-serif';
        const emptyMsg = 'Keine Daten \u2013 bitte Zeitraum w\u00e4hlen und \u201eDaten ziehen\u201c klicken';
        const msgWidth = ctx.measureText(emptyMsg).width;
        const x = Math.max(10, Math.floor(width / 2 - msgWidth / 2));
        const y = Math.floor(height / 2);
        ctx.fillText(emptyMsg, x, y);
        return;
      }
```

This centers the text horizontally and vertically in the canvas, uses the project font (Inter), uses a lighter gray (#9ca3af), and gives a descriptive instruction instead of bare "Keine Daten". The unicode escapes are: \u2013 = en-dash, \u00e4 = ae, \u201e = lower quote, \u201c = upper quote.

**4. Add CSS rule for color picker target size (add to CSS section, before the @media block):**

Insert this CSS rule before the `/* -- Hamburger Button -- */` comment (currently at line 235), so it sits after the mail-log rules (line 233):

```css

    /* ── Color Picker WCAG Target Size ── */
    input[type="color"] { width: 44px; height: 44px; padding: 2px; cursor: pointer; border-radius: var(--radius-sm); border: 1.5px solid var(--clr-border-strong); }
```
  </action>
  <verify>
    <automated>cd /c/Users/nluenzer/git/goaffpro_rechnungen && grep "recipient-advisor-label" src/main/resources/ui/dashboard.html | head -1 && grep "flex-shrink:0" src/main/resources/ui/dashboard.html && grep "emptyMsg" src/main/resources/ui/dashboard.html && grep 'input\[type="color"\]' src/main/resources/ui/dashboard.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "recipient-advisor-label" src/main/resources/ui/dashboard.html | head -1` contains `var(--clr-text)` and does NOT contain `#b91c1c`
    - `grep "flex-shrink:0" src/main/resources/ui/dashboard.html` returns a match (the date range wrapper div)
    - `grep "analyticsFromDate" src/main/resources/ui/dashboard.html | grep "min-width:140px"` returns a match (reduced from 170px)
    - `grep "emptyMsg" src/main/resources/ui/dashboard.html` returns at least one match (the new chart empty state variable)
    - `grep "Keine Daten" src/main/resources/ui/dashboard.html` does NOT appear at line 2295 with `ctx.fillText('Keine Daten', 20, 30)` (old pattern replaced)
    - `grep 'input\[type="color"\]' src/main/resources/ui/dashboard.html` contains `width: 44px` and `height: 44px`
    - `grep "14px Arial" src/main/resources/ui/dashboard.html` returns NO matches (replaced with Inter font)
  </acceptance_criteria>
  <done>
    Beraterinnen-E-Mail label uses neutral text color instead of red. Date range stays on one line via flex wrapper. Chart empty state shows centered descriptive text with Inter font. Color picker inputs are 44x44px minimum.
  </done>
</task>

</tasks>

<verification>
- `grep -c 'rel="icon"' src/main/resources/ui/dashboard.html` returns 1
- `grep -c '<form' src/main/resources/ui/dashboard.html` returns 1
- `grep 'goaffproAPIKey.*type="password"' src/main/resources/ui/dashboard.html` matches
- `grep 'recipient-advisor-label' src/main/resources/ui/dashboard.html` contains `var(--clr-text)`, not `#b91c1c`
- `grep 'flex-shrink:0' src/main/resources/ui/dashboard.html` matches (date wrapper)
- `grep 'emptyMsg' src/main/resources/ui/dashboard.html` matches (chart empty state)
- `grep 'input\[type="color"\]' src/main/resources/ui/dashboard.html` contains `44px`
- `grep 'Anmelden' src/main/resources/ui/dashboard.html` matches (login button)
</verification>

<success_criteria>
- No /favicon.ico 404 in browser console
- Login fields inside <form> with autocomplete attributes
- GoAffPro and ERPNext API-Key inputs masked (type=password)
- Beraterinnen-E-Mail label is not red
- Date range stays on one line at 1440px viewport
- Chart empty state shows centered descriptive German text
- Color picker inputs are 44x44px
- Login button says "Anmelden"
</success_criteria>

<output>
After completion, create `.planning/phases/02-versch-nerung/02-01-SUMMARY.md`
</output>
